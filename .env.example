# Copie para .env (git-ignored) e ajuste. docker-compose e o Taskfile leem daqui.

# PostgreSQL
POSTGRES_DB=lmf_bank
POSTGRES_USER=postgres
POSTGRES_PASSWORD=root

# JWT (o backend exige; mínimo 32 caracteres)
JWT_SECRET=minha-chave-super-secreta-minimo-32-caracteres
JWT_EXPIRATION=86400000
