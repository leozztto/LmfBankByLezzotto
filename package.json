{
  "name": "lmfbank",
  "version": "0.0.0",
  "private": true,
  "description": "Raiz do monorepo — só ferramentas transversais (git hooks, commitlint). O app fica em frontend/, o serviço em backend/.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.6.1",
    "@commitlint/config-conventional": "^19.6.0",
    "husky": "^9.1.7"
  }
}
