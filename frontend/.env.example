# Copie para .env.local (git-ignored) para rodar `next dev` fora do compose.
# Onde o BFF (route handlers) repassa /api/* — ADR 0007.
BACKEND_ORIGIN=http://localhost:8080

# Só envie Secure no cookie de sessão sob HTTPS.
# COOKIE_SECURE=false
